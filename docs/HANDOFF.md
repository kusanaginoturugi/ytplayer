# 引き継ぎ

## 現在の状態

- 起動: `./ytplayer`
- 音声保存先: `~/Music/ytplayer/`
- 保存先変更: `YTPLAYER_MUSIC_DIR=/path/to/music ./ytplayer`
- 保存済みの音声は動画IDを含む名前で識別する。

## 注意点

- ダウンロード失敗時は再生を止めず、ストリーミングへフォールバックする。
- リポジトリには既存の `__pycache__` 追跡ファイルがあり、今回生成されたバイトコード差分はコミットしない。
